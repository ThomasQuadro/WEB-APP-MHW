# ─── PostgreSQL (self-hosted) ────────────────────────────────────────────────
POSTGRES_PASSWORD=change-me-in-production

# ─── JWT Secret ───────────────────────────────────────────────────────────────
# Minimum 32 caractères — changer absolument en production
JWT_SECRET=your-super-secret-and-long-jwt-secret-value-change-in-prod

# ─── Clés JWT dérivées du JWT_SECRET ──────────────────────────────────────────
# Générer avec :  node scripts/generate-jwt.js <JWT_SECRET>
# puis coller les deux lignes produites ci-dessous.
ANON_KEY=
SERVICE_ROLE_KEY=
